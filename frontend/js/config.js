/**
 * API base URL. Netlify (frontend) + Render (backend) için Render servis URL'iniz.
 * Servis adınız farklıysa "thy-rag-api" kısmını değiştirin.
 * Yerel geliştirme (./run.sh): geçici olarak "" yapın ki chat localhost:8000'e gitsin.
 */
window.API_BASE = "https://thy-rag-api.onrender.com";
