/**
 * API base URL. Boş bırakılırsa aynı origin kullanılır (local / tek sunucu).
 * Netlify + Render kullanırken buraya Render backend URL'ini yazın, örn.:
 *   window.API_BASE = "https://thy-rag-api.onrender.com";
 */
window.API_BASE = "";
